The progress bar value keeps getting updated in the `progress` table, but I only
ever see the latest number. I want to store the history of the progress over time
so I can look back at how it changed.

Can you set that up?
