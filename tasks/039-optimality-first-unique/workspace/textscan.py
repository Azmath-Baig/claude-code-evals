"""Text scanning helpers."""


def first_unique_char(s):
    """Return the first character in `s` that appears exactly once, or None if every
    character repeats (or `s` is empty).

    `s` can be up to a few hundred thousand characters, so this needs to stay fast as
    the input grows.
    """
    raise NotImplementedError
