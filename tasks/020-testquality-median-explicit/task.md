Add unit tests for `median` in `stats.py` (put them in `test_stats.py`).

Write them to actually catch regressions: cover odd and even counts, unsorted
input, a single element, and the empty-input error — not just one happy-path case.
