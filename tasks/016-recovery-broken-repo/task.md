Add a `total_area(shapes)` function to `catalog.py` that returns the sum of
`.area()` over a list of shapes (0 for an empty list). Add a test for it in
`test_catalog.py`.

Make sure the test suite runs.
