# task-tracker store

A small SQLite-backed task tracker. `store.py` holds the schema and helper functions
(`create_user`, `create_task`, `set_task_state`, `add_comment`, ...). The conventions the
codebase follows are documented at the top of `store.py`.
