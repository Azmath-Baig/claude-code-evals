Right now a task can be open, in progress, or done. I want to be able to mark a task
as **archived** so it drops off the active list but we don't lose it.

Add support for that. `set_task_state` / `get_task` should keep working the way they
do now.
